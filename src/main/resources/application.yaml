spring:
  datasource:
    username: root
    password: maksi
    url: jdbc:postgresql://localhost:5435/faculties
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8100

logging:
  level:
    org.flywaydb: DEBUG
